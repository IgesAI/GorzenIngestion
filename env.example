# Environment variables for LOCAL DEVELOPMENT only
# For deployment (Railway/Vercel), users enter API keys through the web interface

# OPTIONAL: For local development/testing only
# PINECONE_API_KEY=your_test_key_here
# OPENAI_API_KEY=your_test_key_here

# OPTIONAL: Pinecone Configuration (defaults shown)
PINECONE_CLOUD=aws
PINECONE_REGION=us-east-1

# OPTIONAL: Cache path for Docling models (improves startup time)
DOCLING_ARTIFACTS_PATH=

# NOTE: In production deployment, API keys are entered by users through the web interface
# This design allows anyone to use their own API keys without hardcoding them
